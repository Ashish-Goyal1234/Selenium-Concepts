1. Alerts-- JavaScript popup -- Alert API(accept, dismiss).
2. FileUpload Popup (Windows Popup) -- (type=file, dont use click(), directlu use sendKeys("path"))
3. Browser windows popup -- (windowHandler API - getWindowsHandler() return set<String>).